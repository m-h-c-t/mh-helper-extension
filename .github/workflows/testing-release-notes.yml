name: Release Notes Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: ncipollo/release-action@v1
        with:
          name: "jacks test release draft"
          draft: true
          tag: "jacks-test"
          generateReleaseNotes: true
